import {NetworkMultiVariantDescriptor} from "../descriptor/NetworkMultiVariantDescriptor";

export interface MultiVariantNetwork {
    setMultiVariantDescriptor(descriptor: NetworkMultiVariantDescriptor);
}