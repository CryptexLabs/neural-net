// global.fs = require('fs');
// global.path = require('path');
// global.assert = require('assert');
// global.chai = require('chai');
//
// global.BASE_DIRECTORY = path.resolve(process.argv[1]).split('/node_modules')[0];
//
// global.ENV = 'TEST';
// global.SRC_DIRECTORY = BASE_DIRECTORY + '/src';
// global.HELPER_PATH = SRC_DIRECTORY + '/helpers';
// global.MODEL_PATH = SRC_DIRECTORY + '/models';
// global.QUERY_PATH = SRC_DIRECTORY + '/query';
//
// global.AWS = require('aws-sdk');
// let options = {
// 	"region": "us-west-2",
// 	"endpoint": "http://localhost:8001"
// };
// AWS.config.update(options);
//
// require('../autoload');
//# sourceMappingURL=bootstrap.js.map